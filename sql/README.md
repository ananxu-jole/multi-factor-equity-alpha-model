# SQL Artifacts

This directory holds local SQLite databases and SQL helper scripts.

Commit:
- Schema definitions and reusable SQL scripts.
- Small, intentional migration or exploration queries.

Do not commit:
- Large generated SQLite databases such as `project_underdog.db`.
- Temporary database exports, backups, or local query scratch files.
