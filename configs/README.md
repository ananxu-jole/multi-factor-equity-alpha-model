# Configs

This directory holds human-maintained configuration files for strategy, alpha, and context settings.

Commit:
- Version-controlled JSON/YAML/TOML configs that define reproducible behavior.
- Template config files without secrets.

Do not commit:
- Secrets, credentials, API keys, or personal machine paths.
- Local override files unless they are explicitly sanitized and intended for shared use.
